name: rope
type: library
use: makru_langc
version: 0.1.0-alpha.0
dependencies:
  - czmq
  - msgpack@3.*
actions:
  envinfo: makru_langc:envinfo
  resolve: makru_langc:resolve_dependencies
  run: makru_langc:run
language_c:
  resolvers:
  - pkgconfig
  - system
  sources:
  - src/**
  std: c11
